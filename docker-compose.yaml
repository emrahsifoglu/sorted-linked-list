services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        PHP_VERSION: 8.2
    working_dir: /app
    volumes:
      - ./:/app
      - composer-cache:/tmp/composer
    environment:
      COMPOSER_HOME: /tmp/composer
    profiles:
      - tools

volumes:
  composer-cache:
